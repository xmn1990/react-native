import createIconSet from 'react-native-vector-icons/lib/create-icon-set';
import glyphMap from './iconfont.json';

const EIcon = createIconSet(glyphMap, 'EIcon', 'iconfont.ttf');

export default EIcon;
